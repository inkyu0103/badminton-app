import EmailSend from "components/common/EmailSend";

export default {
  component: EmailSend,
};

export const Default = () => {
  parameters: {
    chromatic: {
      disableSnapshot: true;
    }
  }
};
